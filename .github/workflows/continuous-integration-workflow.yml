name: Continuous Deployment

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build and Deploy To Server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: github-workflow-artifacts
      - name: Install NPM modules and run Gatsby build
        uses: actions/setup-node@v1
        with:
          node-version: '12.17.0'
      - run: cd github-workflow-artifacts/ && npm install
      - run: cd github-workflow-artifacts/ && npm run build
      - name: Copy public directory to the server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          source: "github-workflow-artifacts"
          target: "/root/projects/chrisfrew.in"
          overwrite: true
      - name: Restart (or start when restart fails) node forever on the server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            source /root/.bashrc
            cd "/root/projects/chrisfrew.in/github-workflow-artifacts/"
            forever restart index.js || forever start index.js && echo 'Forever successfully running!'
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@v2.0.0
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_TITLE: "Build complete! Latest commit message:"